/**
 * Request header set by middleware on `/discover/clubs` routes so the root layout and
 * `beforeInteractive` theme script can apply the club’s color theme on first paint.
 */
export const DISCOVER_ROUTE_COLOR_THEME_HEADER =
  "x-influencers-discover-color-theme";
